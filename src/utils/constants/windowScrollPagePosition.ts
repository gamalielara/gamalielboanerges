export enum ItemToShow {
  ROTATING_NAME_BOXES = "ROTATING_NAME_BOXES",
  AIRPLANE = "AIRPLANE",
  MOON = "MOON"
}

export enum WindowScrollPagePosition {
  ROTATING_NAME_BOXES = window.innerHeight,
  AIRPLANE = window.innerHeight * 2,
  MOON = window.innerHeight * 3
}